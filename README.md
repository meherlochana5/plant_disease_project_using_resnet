# plant_disease_project_using_resnet
A plant disease detection project using a fine-tuned ResNet50 model trained on the PlantVillage dataset. Transfer learning is used with custom dense layers added on top of ResNet50. The final model achieves 90%+ accuracy and is deployed using a Flask web app for prediction.
